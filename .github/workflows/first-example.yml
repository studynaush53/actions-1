name: My first Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checksout repo
              uses: actions/checkout@v4

            - name: Welcome message
              run: echo "my first gti hbi act"

            - name: List Files 
              run: ls
            
            - name: Read files 
              run: cat README.md
